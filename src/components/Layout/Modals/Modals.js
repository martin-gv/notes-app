import React from "react";

function Modals() {
  return (
    <>
      {/* <NewNoteModal show={show.newNote} close={() => toggleModal("newNote")} /> */}
      {/* <NewTagModal show={show.newTag} close={() => toggleModal("newTag")} /> */}
    </>
  );
}

export default Modals;
